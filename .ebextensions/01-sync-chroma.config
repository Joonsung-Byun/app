commands:
  sync_chroma_data:
    command: |
      BUCKET_NAME="chromadb-aigo-team4"
      LOCAL_PATH="/var/app/chroma_data"

      mkdir -p "${LOCAL_PATH}"
      aws s3 sync "s3://${BUCKET_NAME}/chroma_data" "${LOCAL_PATH}" --delete